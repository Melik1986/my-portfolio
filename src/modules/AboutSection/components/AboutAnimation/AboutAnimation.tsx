'use client';

import { AboutAnimationProps } from '@/types/about.types';

export const AboutAnimation: React.FC<AboutAnimationProps> = (props) => {
  return (
    <div className="about__animation" id="aurora-container">
      {/* Canvas будет добавлен сюда с помощью JavaScript */}
    </div>
  );
};
